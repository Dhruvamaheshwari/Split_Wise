# ==========================================
# Backend Environment Variables Template
# ==========================================

# 1. Database (Neon DB / Prisma)
# Replace with your Neon DB connection strings
DATABASE_URL="postgresql://neondb_owner:npg_i1zVC4gRPecT@ep-quiet-sea-ao0wl3vk-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# 2. Authentication (Auth.js for Express)
# Generate a random 32-character string for AUTH_SECRET (e.g., using `openssl rand -base64 32`)
AUTH_SECRET="your_generated_random_secret_here"
AUTH_URL="http://localhost:3000/api/auth"

# 3. Realtime Chat (Pusher)
# Get these from your Pusher.com App Dashboard
PUSHER_APP_ID="your_pusher_app_id"
PUSHER_KEY="your_pusher_key"
PUSHER_SECRET="your_pusher_secret"
PUSHER_CLUSTER="your_pusher_cluster"
